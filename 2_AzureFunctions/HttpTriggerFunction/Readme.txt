The output functionAppDefaultHostName was https://defaultDomain/api/Function1.

The 3rx47yhxqmxc6 was the output of the uniqueString method in createResources.bicep.

The URLs to trigger the other two functions are
http://defaultDomain/api/CreatingResponseExplicitly
http://defaultDomain/api/ReadingFromRequest/vasya

Make sure your App service plan is on Y1 pricing. That is the free plan for Azure Function hosting.